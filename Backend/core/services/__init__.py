"""
Cross-domain business logic lives here.
Views stay thin — complex logic goes into service classes.

Example:
    from core.services.order_service import OrderService
    OrderService.split_into_suborders(order)
"""
